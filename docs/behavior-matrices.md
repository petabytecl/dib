# Behavior Matrices

This is the initial shared behavior contract matrix for Dib. Package tests
remain the executable truth. This document records the cross-surface hooks that
keep `command/`, `flags/`, and `config/` from drifting as each surface grows.

## Shared Contracts

| Contract | Applies To | Required Behavior | Initial Evidence | Later Hook |
| --- | --- | --- | --- | --- |
| Immutable definitions | `command`, `flags`, `config` | Definitions and derived definitions return new values instead of mutating existing values. Exported APIs do not expose mutable internals. | Story 1.1 `command.NewDefinition` returns a value with unexported fields; Story 3.1 `TestDefinitionDerivationDoesNotMutateOriginalsOrLeakAliases` covers command metadata and children; Story 2.1 `flags.Set.With` returns a derived set without mutating the original; Story 4.1 `TestSetDerivationDoesNotMutateOriginals` covers config set derivation and normalizer derivation. | Add derivation assertions for future config source-binding inputs when those APIs exist. |
| No mutable aliases | `command`, `flags`, `config` | Constructors and derivation methods defensively handle caller-owned slices, maps, readers, environment lookup functions, and config data. Callers must not observe mutation through retained aliases after construction or resolution. | Story 3.1 `TestDefinitionDerivationDoesNotMutateOriginalsOrLeakAliases`, `TestRouteSnapshotsAreDefensiveAndDeterministic`, and `TestRouteResultExposesDefensivePathDefinitions` prove command aliases, child slices, paths, and remaining args are defensive; Story 3.2 `TestRouteResultExposesDefensiveMatchTokens` adds raw match-token snapshot coverage; Story 3.5 `TestBoundaryAccessorsAreDefensiveAndReusable` proves boundary args and route results remain defensive; Story 2.1 tests prove `flags.StringList` defaults, `Set.Definitions`, and default snapshots do not leak caller-observable mutable storage; Story 4.1 `TestDefinitionsExposeMetadataAndDefensiveDefaults` and `TestDefaultSnapshotIsReusableAndDefensive` prove config string-list defaults and snapshots are defensive; Story 4.2 `TestExplicitSnapshotLastWriterWinsAndDefensiveValues` and `TestJSONSnapshotDefensiveStringLists` prove source snapshots clone caller and JSON string-list values. | Add table tests for each new caller-owned input as config APIs are introduced. |
| Per-run snapshots | `command`, `flags`, `config` | Route, parse, and config resolution snapshots never write back to definitions. A snapshot copies the values it needs and does not depend on live process state, environment variables, readers, lookup functions, or caller-owned mutable inputs after creation. | Story 3.1 `TestRouteSnapshotsAreDefensiveAndDeterministic` proves route snapshots are self-contained; Story 3.2 extends the same test for alias route calls and raw match tokens; Story 2.1 `flags.Set.DefaultSnapshot` tests prove default value state is self-contained and ignores misleading `os.Args` and environment state; Story 4.1 `TestDefaultSnapshotResolvesDefaultsAndNotFound`, `TestDefaultSnapshotIsReusableAndDefensive`, and `TestDefaultSnapshotConcurrentReuse` prove config default snapshots are self-contained and reusable; Story 4.2 `TestEnvSnapshotUsesInjectedLookupAndTracksMetadata`, `TestEnvSnapshotAbsentVariablesAreNotErrors`, and `TestJSONReaderSnapshotStrictAndPermissiveModes` prove source snapshots are built from explicit env lookups and JSON inputs. | Extend snapshot assertions when config source precedence exists. |
| Explicit instances | `command`, `flags`, `config` | Primary APIs accept caller-provided values and instances. They do not read package globals, default singletons, `os.Args`, environment variables, stdin, stdout, or stderr unless that behavior is explicit in the API under test. | `command/contract_test.go` demonstrates explicit command definition construction; Story 3.1 `TestRoutingUsesExplicitInputsAndReturnedValues` proves routing uses supplied args and returned values only; Story 3.5 `TestRouteBoundaryPackagesExplicitCallerInputs` proves boundary routing uses caller args, context, and writers while ignoring misleading process state; Story 2.1 flags tests demonstrate independent `flags.Set` construction despite misleading process state; Story 4.1 config constructors, `NewSet`, and `DefaultSnapshot` use only caller-supplied definitions and returned values; Story 4.2 `TestExplicitSnapshotLastWriterWinsAndDefensiveValues`, `TestEnvSnapshotUsesInjectedLookupAndTracksMetadata`, and `TestJSONPathSnapshotUsesCallerSuppliedPathAndFixtures` prove config source ingestion uses explicit assignments, injected lookup, readers, and caller-supplied paths. | Extend the same pattern when config flag-binding inputs are added. |
| Explicit flag name normalization | `flags` | Long flag names match exactly unless a caller supplies a `flags.NameNormalizer`. Normalized sets resolve equivalent long-name spellings to the canonical definition name, reject normalized collisions with typed setup errors, and keep shorthand lookup independent from long-name normalization. | Story 2.2 tests cover exact matching, configured normalization, normalized collision diagnostics, immutable derivation with `WithNormalizer`, and shorthand separation. | Extend parsing tests to reuse the same lookup contract once flag-token parsing exists. |
| Long flag parsing | `flags` | `flags.Set.Parse(args)` accepts caller-supplied arguments, parses long flags in attached and separate value forms, treats boolean presence as true, preserves remaining positional args, records source occurrences, and returns typed diagnostics for unknown, missing, conversion, and duplicate-value failures. | Story 2.3 tests cover `--name=value`, `--name value`, booleans, `--no-*` as an ordinary name, exact and normalized lookup, duplicate single-value flags, minimal `--` terminator behavior, source metadata, defensive snapshots, and redaction-safe parse errors. | Extend the parser matrix for shorthand, short groups, repeat/custom accumulation, and complete boundary behavior in later Epic 2 stories. |
| Short flag parsing | `flags` | `flags.Set.Parse(args)` resolves one-rune shorthand definitions through the set shorthand index, parses required values in `-n value` and `-n=value` forms, treats boolean `-v` presence as true, preserves remaining positional args and `--` protection, records canonical source occurrences, and returns typed diagnostics for unknown shorthand, missing values, conversion failures, and duplicates across long/short spellings. | Story 2.4 tests cover valid short values, boolean presence, positionals around short flags, unknown shorthand, missing values, invalid conversions, duplicate values, normalized long-name independence, `--` protection, canonical occurrence metadata, and redaction-safe shorthand conversion errors. | Add grouped shorthand behavior, optional group values, full boundary matrices, repeated/custom accumulation proof, and fuzz/property tests in later Epic 2 stories. |
| Grouped shorthand parsing | `flags` | Single-dash tokens with multiple shorthand members and no `=` parse as shorthand groups. Boolean members are applied left to right; a final required-value member may consume an attached suffix such as `-an10` or the next argument in `-an 10`; a no-option default is applied when a grouped member is present without an explicit value; a non-final required-value member without a no-option default returns `flags.ErrInvalidGroup`. | Story 2.5 tests cover boolean groups, final attached and separate values, no-option defaults, invalid non-final required members, unknown members, invalid conversions, duplicate values within and across grouped/long spellings, partial-failure behavior, `--` protection, source occurrence metadata, and deterministic grouped-input fuzz/property checks. | Add complete repeated/custom accumulation, full parse boundary behavior, final parser fuzz matrices, compatibility tables, and release evidence in later Epic 2 and Epic 5 stories. |
| Repeated and custom values | `flags` | Repeated occurrences accumulate only for definitions configured with `flags.Repeatable()`, preserving command-line order across long, short, grouped final shorthand, and no-option default forms. `ValueState.Values()` exposes effective values; `ValueState.Occurrences()` exposes one provenance record per explicit CLI occurrence. `KindStringList` parser results flatten into effective values while retaining one occurrence per CLI token. Single-value duplicates are rejected before converting the second value. Custom parsers store validated, defensively copied values and preserve non-sensitive parser causes through Go error inspection; sensitive custom failures redact raw values and caller parser causes. | Story 2.6 tests cover mixed-spelling accumulation, no-option repeated accumulation, duplicate rejection before conversion across long, short, grouped, normalized, and mixed spellings, scalar custom parser success, custom `KindStringList` flattening, mismatched custom result kinds, non-sensitive parser wrapping, sensitive redaction, and reusable parse runs with mutable parser results. | Add full parse boundary behavior, final parser fuzz matrices, compatibility tables, migration examples, and release evidence in later Epic 2 and Epic 5 stories. |
| Parse boundaries | `flags` | Positional arguments interspersed with flags accumulate in `RemainingArgs()` in their relative order. The `--` terminator stops flag parsing; every token after `--` is preserved verbatim in `RemainingArgs()` even if it looks like a flag. Help requests (`--help` or `-h` when not registered by the caller) return a typed `ErrHelpRequest` diagnostic for caller-controlled rendering and exit policy; no runtime path calls `os.Exit`. Parse failures (unknown flag, missing value, invalid value, invalid group, duplicate) return a typed `*flags.ParseError` and a zero-value `Snapshot` so callers cannot accidentally use partial state. | Story 2.7 tests cover positional-only args, flags-only args, interleaved positionals and flags, multiple positionals between flags, `--` alone, `--` followed by flag-like tokens, `--` as the first arg, `--` after parsed flags, passthrough args after `--`, each parse-failure category with `errors.Is`/`errors.As` typed assertions, zero-value snapshot on failure, registered `--help`/`-h` parsing normally, and a fuzz harness (`FuzzParseBoundary`) with clean-room seed corpus verifying no panic and boundary invariants. | Add full parser fuzz matrix proof, compatibility tables, and release evidence in Story 2.8 and Epic 5 stories. |
| Public error inspection | `command`, `flags`, `config` | Errors that callers need to handle programmatically are typed or sentinel errors inspectable with `errors.Is` or `errors.As`. Error strings are diagnostics, not the programmatic contract. | Story 1.1 uses `errors.As` for `*command.NameError`; Story 3.1 `TestRouteUnknownCommandErrorsAreInspectable` uses `errors.Is(command.ErrUnknownCommand)` and `errors.As` with `*command.UnknownCommandError`; Story 3.2 `TestDefinitionRejectsInvalidAliases`, `TestDefinitionRejectsAliasTokenCollisions`, `TestTokenConflictErrorParentPathIsDefensive`, and `TestAliasErrorParentPathIsDefensive` cover alias setup diagnostics; Story 2.1 uses sentinel errors with `*flags.DefinitionError` and `*flags.ValueError`; Story 4.1 `TestSetValidationErrorsAreInspectable`, `TestNormalizedSetLookupAndCollisions`, and `TestNormalizedSetRejectsInvalidNormalizedKeys` cover config setup diagnostics; Story 4.2 `TestExplicitSnapshotValidationErrorsAreTypedAndNoPartialSnapshot`, `TestEnvSnapshotDiagnosticsAreTypedAndRedacted`, and `TestJSONDiagnosticsAreTypedAndRedacted` cover config source diagnostics. | Add typed error assertions for future config precedence failures. |
| Diagnostic vocabulary | `command`, `flags`, `config` | Human-facing diagnostics identify the package surface, command/flag/key name when applicable, typed category, source label when applicable, and redaction status. | `docs/diagnostics-and-errors.md` defines the vocabulary shape. | Add source report and rendered diagnostic tests when diagnostics are implemented. |
| Source labels | `config` | Config provenance uses exactly `default`, `explicit setter`, `flag binding`, `env`, and `JSON`. | `docs/diagnostics-and-errors.md` fixes the vocabulary; Story 4.1 default snapshots use `default`; Story 4.2 `TestExplicitSnapshotLastWriterWinsAndDefensiveValues`, `TestEnvSnapshotUsesInjectedLookupAndTracksMetadata`, and `TestJSONReaderSnapshotStrictAndPermissiveModes` prove `explicit setter`, `env`, and `JSON` source labels. | Add precedence winner tests when config precedence exists. |
| Redaction corpus | `command`, `flags`, `config` | The fake sensitive values are exactly `dib_fake_secret_value`, `dib_fake_password_value`, and `dib_fake_token_value`. Raw sensitive values must not appear in public diagnostics once redaction behavior exists. | `docs/diagnostics-and-errors.md` fixes the corpus and redaction scope; Story 2.1 proves sensitive flag conversion errors omit `dib_fake_secret_value`; Story 4.1 `TestSensitiveDefaultDiagnosticsDoNotExposeFakeCorpus` proves sensitive config default setup diagnostics omit all three corpus values. | Add redaction assertions for debug strings, examples, and source reports as those surfaces exist. |

## Flag Parser Evidence Map

This section provides the Epic 2 parser evidence map with FR traceability and
executable test evidence. Each row names the behavior area, its functional
requirement IDs, the expected behavior, and the exact test or fuzz functions
that prove it. Status `current` means evidence exists in the repository today;
`later` means the behavior area depends on command routing, config binding, or
release-candidate validation outside Epic 2 scope.

This document does not claim source compatibility with Go `flag`, pflag, Cobra,
or Viper. Behavior-scoped familiarity with those APIs informed the design, but
no source, tests, comments, examples, fixtures, internal names, or file
organization were copied or adapted from them.

| Behavior Area | FR IDs | Expected Behavior | Executable Evidence | Status |
| --- | --- | --- | --- | --- |
| Reusable flag definitions and immutable sets | FR5 | `flags.NewSet` and `flags.Set.With` return new independent sets; definitions expose immutable metadata; derived sets do not mutate originals; `DefaultSnapshot` returns self-contained state without reading `os.Args` or environment state. | `TestATDDFlagDefinitionsExposeMetadata`, `TestDefinitionsExposeMetadata`, `TestATDDFlagSetValidationErrorsAreInspectable`, `TestValidationErrorsAreInspectable`, `TestNewSetValidationErrors`, `TestSetWithReturnsIndependentSet`, `TestATDDIndependentFlagSetsIgnoreAmbientProcessState`, `TestIndependentFlagSetsIgnoreAmbientProcessState`, `TestATDDDerivedFlagSetsDoNotMutateOriginalsOrLeakAliases`, `TestDerivedFlagSetsDoNotMutateOriginalsOrLeakAliases` | current |
| Normalization and exact matching | FR10 | Long flag names match exactly by default; a caller-supplied `NameNormalizer` resolves equivalent spellings to the canonical name; normalized collisions produce typed `*flags.DefinitionError` with `ErrDuplicateNormalizedName`; shorthand lookup is independent from long-name normalization. | `TestNameNormalizationLookupModes`, `TestNameNormalizationValidationErrors`, `TestATDDExactFlagNamesRemainDistinctByDefault`, `TestExactFlagNamesRemainDistinctByDefault`, `TestATDDConfiguredNormalizerResolvesCanonicalDefinitions`, `TestConfiguredNormalizerResolvesCanonicalDefinitions`, `TestATDDNormalizationCollisionsAreInspectable`, `TestNormalizationCollisionsAreInspectable`, `TestATDDNormalizedDerivationDoesNotMutateOriginalSets`, `TestNormalizedDerivationDoesNotMutateOriginalSets`, `TestATDDLongNameNormalizationDoesNotCreateShorthandAliases`, `TestLongNameNormalizationDoesNotCreateShorthandAliases`, `TestWithNormalizerAndWithKeepSetsIndependent` | current |
| Long flag parsing | FR6 | `Set.Parse` accepts `--name=value`, `--name value`, and boolean presence `--flag`; `--no-*` is an ordinary name; exact and normalized lookup resolve to the canonical definition; duplicate single-value flags return `ErrDuplicateValue` before conversion; occurrences record the source spelling; long-flag parsing uses `stopBeforeLong=true` so a following long-flag token is never consumed as a value. | `TestATDDLongFlagValuesPreserveSourceAndRemainingArgs`, `TestLongFlagValuesPreserveSourceAndRemainingArgs`, `TestATDDBooleanLongFlagsParsePresenceAndExplicitValues`, `TestBooleanLongFlagsParsePresenceAndExplicitValues`, `TestATDDUnknownLongFlagsExposeLookupContext`, `TestUnknownLongFlagsExposeLookupContext`, `TestATDDMissingRequiredLongValuesAreInspectableAndLeaveSetReusable`, `TestMissingRequiredLongValuesAreInspectableAndLeaveSetReusable`, `TestATDDDuplicateSingleValueLongFlagsAreInspectable`, `TestDuplicateSingleValueLongFlagsAreInspectable`, `TestATDDExactAndNormalizedLongNamesParseSafely`, `TestExactAndNormalizedLongNamesParseSafely`, `TestATDDNoPrefixedLongNamesAreOrdinaryNames`, `TestNoPrefixedLongNamesAreOrdinaryNames`, `TestATDDSensitiveConversionErrorsDoNotLeakAttachedValues`, `TestSensitiveConversionErrorsDoNotLeakAttachedValues`, `TestParseLongFlagsCoversContractMatrix`, `TestParseLongSnapshotAccessorsAreDefensive`, `TestParseLongErrorsDoNotEchoRawValues`, `TestParseLongBooleanExplicitTrue`, `TestParseLongNormalizedOccurrenceExposesLookupKey`, `TestParseLongDuplicateSingleValuePrecedesConversion`, `TestParseLongDuplicateNormalizedNameKeepsRawContext`, `TestParseLongSeparateSensitiveValuesDoNotLeakInErrors`, `TestParseLongNoOptionDefault` | current |
| Short flag parsing | FR6 | `-n value`, `-n=value`, and boolean presence `-v`; shorthand lookup resolves to the canonical definition; occurrences record shorthand spelling; duplicate values return `ErrDuplicateValue`; shorthand lookup is independent from long-name normalization; short-flag parsing uses `stopBeforeLong=false` so a following long-flag token is consumed as a value. | `TestParseShorthandValuesAndBooleanPresence`, `TestParseShorthandDiagnosticsAreTyped`, `TestParseShorthandRepeatableValuesAccumulate`, `TestParseShorthandFailedParseDoesNotAffectLaterParse`, `TestParseShorthandLookupIsIndependentFromLongNameNormalization`, `TestParseShorthandSensitiveValuesDoNotLeakInErrors`, `TestParseShorthandTerminatorProtectsLaterTokens`, `TestParseShorthandNoOptionDefault` | current |
| Shorthand groups | FR7 | `-abc` tokens parse boolean members left to right; the final member may consume an attached suffix or the next arg; no-option defaults are applied to non-final members whose no-option default is set and the next member is a known shorthand; a non-final required-value member without a no-option default returns `ErrInvalidGroup`; `-h` inside a group returns `ErrUnknownFlag`, not `ErrHelpRequest`. | `TestParseShortGroupBooleanMembers`, `TestParseShortGroupFinalRequiredValue`, `TestParseShortGroupNonFinalNoOptionDefault`, `TestParseShortGroupFinalNoOptionDefaultWithoutExplicitValue`, `TestParseShortGroupDiagnosticsAreTyped`, `TestParseShortGroupTerminatorProtectsLaterTokens`, `TestParseShortGroupRepeatableValuesAccumulate`, `TestParseShortGroupKeepsShorthandIndependentFromLongNormalization`, `FuzzParseShortGroups` | current |
| Repeated and custom values | FR8 | Repeatable flags accumulate occurrences across long, short, grouped, and no-option forms; `ValueState.Values()` returns effective values; `ValueState.Occurrences()` returns one record per CLI occurrence; single-value duplicates are rejected before conversion; custom parsers store defensively copied results; `KindStringList` flattens into effective values; sensitive custom failures redact raw values and parser causes. | `TestParseRepeatableValuesAccumulateAcrossSpellings`, `TestParseRepeatableBuiltInValuesAccumulateByKind`, `TestParseRepeatableCustomValuesAppendOnePerOccurrence`, `TestParseCustomStringListFlattensValuesAndRecordsOccurrences`, `TestParseDuplicateSingleValuePrecedesSecondConversionAcrossSpellings`, `TestParseCustomFailurePreservesNonSensitiveInspection`, `TestParseSensitiveCustomFailureRedactsRawValueAndCause`, `TestParseCustomMutableResultsDoNotLeakAcrossRuns`, `TestParseCustomRejectsMismatchedResultKindThroughParseContract` | current |
| No-option defaults | FR6, FR7 | A flag with `NoOptionDefault` configured applies that value when its token appears without an explicit value. For long flags: at end of args, before `--`, or before another long-flag token (stopBeforeLong applies). For standalone short flags: at end of args or before `--`. For non-final group members: when the next rune is a known shorthand. | `TestParseShortGroupNonFinalNoOptionDefault`, `TestParseShortGroupFinalNoOptionDefaultWithoutExplicitValue`, `TestParseShorthandNoOptionDefault`, `TestParseLongNoOptionDefault` | current |
| Parse boundaries and remaining args | FR9 | Positionals interspersed with flags accumulate in `RemainingArgs()` in relative order; `--` stops flag parsing and preserves all subsequent tokens verbatim; failed parses return zero-value `Snapshot` so callers cannot use partial state; `RemainingArgs()` and snapshot values return defensive copies. | `TestParseBoundaryInterspersedPositionals`, `TestParseBoundaryTerminator`, `TestParseBoundaryPassthrough`, `TestParseBoundaryFailedParses`, `TestParseBoundaryDeterministicSnapshot` | current |
| Help requests | FR9 | Unregistered `--help` or `-h` return `ErrHelpRequest` as a typed `*ParseError`; registered `--help`/`-h` parse normally; `--help=value` with unregistered help flag returns `ErrHelpRequest`; `--help` after `--` is passthrough; `-h` inside a shorthand group (e.g. `-vh`) returns `ErrUnknownFlag`, not `ErrHelpRequest`; help-request parses return zero-value `Snapshot`. | `TestParseHelpLongUnregistered`, `TestParseHelpShortUnregistered`, `TestParseHelpLongRegistered`, `TestParseHelpShortRegistered`, `TestParseHelpAfterTerminator`, `TestParseHelpRequestZeroValueSnapshot`, `TestParseHelpRequestNormalizedName`, `TestParseHelpRequestDefinitionAccessor`, `TestParseHelpLongAttachedValueUnregistered`, `TestParseHelpShortInGroupDoesNotTriggerHelpRequest`, `TestSetReusableAfterHelpRequest` | current |
| Diagnostic error inspection | FR6, FR7, FR8, FR9 | Parse errors are typed `*flags.ParseError` inspectable with `errors.Is` and `errors.As`; `Category()` returns the sentinel; `Token()`, `Name()`, `NormalizedName()`, and `Definition()` expose parse context; definition errors are typed `*flags.DefinitionError`; error strings are diagnostics, not programmatic contracts. | `TestParseBoundaryFailedParses`, `TestSensitiveParseErrorDoesNotExposeRawValue`, `TestNonSensitiveParseErrorWrapsParserCause`, `TestParseShorthandDiagnosticsAreTyped`, `TestParseShortGroupDiagnosticsAreTyped`, `TestATDDValueAndDiagnosticFoundationIsMachineReadable`, `TestValueAndDiagnosticFoundationIsMachineReadable` | current |
| Redaction | FR8, NFR7 | Sensitive parse failures and custom parser failures do not expose raw values in error strings, `ParseError.Token()`, or `ParseError.Error()` output; non-sensitive parser causes remain inspectable via `errors.As`; fake sensitive corpus values: `dib_fake_secret_value`, `dib_fake_password_value`, `dib_fake_token_value`. | `TestSensitiveParseErrorDoesNotExposeRawValue`, `TestNonSensitiveParseErrorWrapsParserCause`, `TestATDDSensitiveConversionErrorsDoNotLeakAttachedValues`, `TestSensitiveConversionErrorsDoNotLeakAttachedValues`, `TestParseLongSeparateSensitiveValuesDoNotLeakInErrors`, `TestParseLongErrorsDoNotEchoRawValues`, `TestParseShorthandSensitiveValuesDoNotLeakInErrors`, `TestParseSensitiveCustomFailureRedactsRawValueAndCause`, `FuzzParse` | current |
| Fuzz and property hardening | FR22, NFR2, NFR4 | Standard-library fuzz targets (`testing.F`, `f.Add`, `testdata/fuzz/`) verify parser invariants on arbitrary inputs: no panic, deterministic success/failure on repeated parses, reusable `Set` definitions are not mutated, successful parses return defensively copied snapshot values, failed parses return zero-value snapshots, typed failures remain inspectable via `errors.As(*ParseError)`. Seeds are independently written clean-room inputs. | `FuzzParseBoundary` (`flags/testdata/fuzz/FuzzParseBoundary/`), `FuzzParseShortGroups`, `FuzzParse` (`flags/testdata/fuzz/FuzzParse/`) | current |
| Command tree definitions | FR1, FR20 | `command.NewDefinition` remains source-compatible while accepting metadata options; command definitions expose names, descriptions, aliases, usage metadata, and children through immutable value APIs and defensive accessors. | `TestNewDefinitionAcceptsStableName`, `TestDefinitionDerivationDoesNotMutateOriginalsOrLeakAliases`, `TestDefinitionRejectsBlankChildNames` | current |
| Command route snapshots | FR1, FR20 | `Definition.Route(args)` accepts explicit caller args, matches canonical root and nested child names, resolves direct child aliases, returns deterministic route snapshots with canonical path names, raw match tokens, and remaining args, and preserves `--` boundary behavior by omitting the marker and returning post-terminator tokens. | `TestRouteRootAndNestedCommands`, `TestRouteRootWithoutChildrenPreservesRemainingArgs`, `TestRouteAliasesExposeCanonicalPathAndRawMatchTokens`, `TestRouteSnapshotsAreDefensiveAndDeterministic`, `TestRouteResultExposesDefensivePathDefinitions`, `TestRouteResultExposesDefensiveMatchTokens` | current |
| Command alias validation | FR1, FR20 | Command setup rejects ambiguous lookup tokens before routing. Blank aliases and self-aliases return `command.ErrInvalidCommandAlias` through `*command.AliasError`; duplicate child names, duplicate aliases, alias-vs-child-name collisions, and cross-alias cycles return `command.ErrDuplicateCommandToken` through `*command.TokenConflictError`. | `TestDefinitionRejectsInvalidAliases`, `TestDefinitionRejectsAliasTokenCollisions`, `TestDefinitionDerivationValidationDoesNotMutateOriginals`, `TestTokenConflictErrorParentPathIsDefensive`, `TestAliasErrorParentPathIsDefensive` | current |
| Command unknown diagnostics | FR1, FR20 | Unknown command tokens return a zero-value route result plus an inspectable `*command.UnknownCommandError` satisfying `errors.Is(err, command.ErrUnknownCommand)`. The error exposes the failing token and matched parent path without requiring string matching, including failures near aliases and under alias-matched parents. | `TestRouteUnknownCommandErrorsAreInspectable`, `TestRouteUnknownCommandNearAliasesIsInspectable`, `TestUnknownCommandErrorParentPathIsDefensive` | current |
| Command local and inherited flag composition | FR3, FR20 | Command definitions attach local and inherited `flags.Definition` values without global state. Route results expose the final composed flag set and parsed snapshot. Inherited flags compose root-to-leaf, final-command local flags compose last, sibling local flags and ancestor local flags remain isolated, and conflicts in long names, shorthands, or normalized names return typed setup diagnostics. | `TestRouteComposesInheritedAndLocalFlags`, `TestRouteKeepsSiblingAndAncestorLocalFlagsIsolated`, `TestRouteFlagCompositionConflictsAreInspectable`, `TestRouteFlagSnapshotsAreDefensiveAndReusable` | current |
| Command flag parser boundaries | FR3, FR20 | Command routing consumes `flags.Set.Parse` behavior for registered flags while preserving canonical command path and raw match-token behavior. Interspersed flags and command tokens route deterministically; `--` stops flag parsing and passes subsequent tokens through as remaining args; help, unknown flag, missing value, conversion, and duplicate-value failures remain typed `flags.ParseError` diagnostics with zero-value route results. | `TestRoutePreservesParserBoundariesAndTypedFlagFailures`, `TestRouteComposesInheritedAndLocalFlags`, `TestAliasRoutingPublicAPIWorkflow` | current |
| Command process boundaries | FR1, FR3, FR20 | Command routing never reads `os.Args` or environment state, writes to stdout/stderr, calls `os.Exit`, invokes callbacks, or takes process lifecycle ownership, including alias routing and flag-aware routing paths. | `TestRoutingUsesExplicitInputsAndReturnedValues`, `TestFlagRoutingUsesExplicitInputsAndReturnedValues` | current |
| Command help and usage rendering | FR4, FR20 | Command definitions and route results render deterministic usage/help text to caller-supplied `io.Writer` values. Output includes canonical names, aliases, descriptions, usage metadata, child commands, and visible flags in definition/composition order. Hidden flags are omitted from output while remaining parseable through `Route`; visible deprecated flags render deterministic notes; sensitive defaults and parse values are not rendered. | `TestWriteHelpRendersDefinitionMetadataDeterministically`, `TestWriteHelpRendersDefinitionLocalFlagsWithoutRoute`, `TestWriteHelpRendersRoutedCommandFlagsAndUsage`, `TestWriteHelpOmitsHiddenAndSensitiveValues`, `TestWriteHelpDoesNotChangeRouteHelpRequestBehavior` | current |
| Command rendering boundaries | FR4, FR20 | Rendering does not route, parse, execute callbacks, read `os.Args` or environment state, write to stdout/stderr, call `os.Exit`, use terminal width or host-specific state, or mutate command definitions and route results. Writer failures are returned directly, and zero-value definitions/results return inspectable name setup errors. | `TestWriteHelpUsesSuppliedWriterOnly`, `TestWriteHelpPropagatesWriterFailuresAndRejectsInvalidDefinitions`, `TestWriteUsagePropagatesWriterFailuresAndRejectsInvalidTargets`, `TestWriteHelpIsRepeatableConcurrentAndDefensive` | current |
| Command execution-boundary metadata | FR2, FR20 | `command.Boundary` packages an existing route result with caller-supplied context, args, stdout, and stderr metadata. `Definition.RouteBoundary` routes explicit args and returns the same typed routing or flag parse errors as `Route`. Boundary values do not execute callbacks, write to supplied writers, read process state, decide exit policy, convert ordinary caller errors, or duplicate route snapshot state. Zero-value boundaries report absent state through boolean accessors rather than panicking. | `TestRouteBoundaryPassesHelpRequestsWithoutRendering`, `TestRouteBoundaryLeavesRenderingUnderCallerControl`, `TestRouteBoundaryPreservesTerminatorPassthrough`, `TestRouteBoundaryPackagesExplicitCallerInputs`, `TestBoundaryPreservesCanceledContextWithoutPolicy`, `TestBoundaryRetainsWritersWithoutWriting`, `TestRouteBoundaryPreservesTypedRoutingAndFlagErrors`, `TestBoundaryKeepsOrdinaryCallerErrorsSeparate`, `TestBoundaryAccessorsAreDefensiveAndReusable`, `TestBoundaryZeroValueIsAbsentState` | current |
| Config key definitions | FR11, FR16, FR20 | Config definitions expose stable key names, type expectations, optional defaults, usage text, and sensitivity metadata. Exact matching is the default; configured normalization resolves equivalent spellings, rejects invalid normalized keys, and rejects normalized collisions with typed setup errors. `DefaultSnapshot` returns defaults with `default` provenance, distinguishes no-default registered keys from zero defaults, and returns a documented not-found boolean for unregistered keys. | `TestDefinitionsExposeMetadataAndDefensiveDefaults`, `TestKindStringVocabulary`, `TestSetExactLookupAndDefinitionsAreDeterministic`, `TestSetValidationErrorsAreInspectable`, `TestNormalizedSetLookupAndCollisions`, `TestNormalizedSetRejectsInvalidNormalizedKeys`, `TestSetDerivationDoesNotMutateOriginals`, `TestDefaultSnapshotResolvesDefaultsAndNotFound`, `TestDefaultSnapshotIsReusableAndDefensive`, `TestDefaultSnapshotUsesSetNormalizer`, `TestDefaultSnapshotConcurrentReuse`, `TestSensitiveDefaultDiagnosticsDoNotExposeFakeCorpus`, `TestNonSensitiveDefaultDiagnosticsMayExposeOrdinaryInvalidValues`, `TestQAConfigPublicWorkflowCoversDefaultsNormalizationAndNotFound`, `TestQAConfigSetupErrorsCoverUnknownKindsAndNormalizedCollisions` | current |
| Config source ingestion | FR12, FR14, FR15, FR20 | Explicit assignments, injected env lookup, and explicit JSON readers/paths produce source snapshots for registered keys without resolving cross-source precedence. Explicit repeated valid writes are deterministic last-writer-wins. Env bindings use explicit names or configured prefix/replacer mapping and treat present empty values as set. JSON loading accepts object documents, defaults to strict registered-key mode, supports permissive unknown-key mode, and reports read/decode/unknown/type failures through typed diagnostics. | `TestExplicitSnapshotLastWriterWinsAndDefensiveValues`, `TestExplicitSnapshotValidationErrorsAreTypedAndNoPartialSnapshot`, `TestExplicitSnapshotZeroValuesAreSetValues`, `TestEnvSnapshotUsesInjectedLookupAndTracksMetadata`, `TestEnvSnapshotAbsentVariablesAreNotErrors`, `TestEnvSnapshotDiagnosticsAreTypedAndRedacted`, `TestJSONReaderSnapshotStrictAndPermissiveModes`, `TestJSONPathSnapshotUsesCallerSuppliedPathAndFixtures`, `TestJSONDiagnosticsAreTypedAndRedacted`, `TestJSONDiagnosticsUseDeterministicKeyOrder`, `TestJSONSnapshotDefensiveStringLists`, `TestQAConfigSourceWorkflowCoversExplicitEnvAndJSONBoundaries`, `TestQAConfigSourceDiagnosticsCoverCriticalFailuresAndRedaction` | current |
| Config precedence resolution | FR12, FR13, FR20 | `config.Resolve` applies the V1 precedence order (explicit setter > flag binding > env > JSON > default) across independent source snapshots. A zero-value `Snapshot{}` for any tier is safe. `NewFlagSnapshot` ingests caller-supplied `FlagValue` bindings with `flag binding` provenance; only explicitly-set flags contribute values; duplicate bindings and unknown keys are setup errors. The returned snapshot is self-contained and immutable. | `TestResolvePrecedenceAdjacentPairs`, `TestResolveZeroValueSnapshotsBehaviorEqualsDefaultSnapshot`, `TestResolveFlagDefaultDoesNotOverrideEnv`, `TestResolveFlagExplicitBeatsEnvAndJSON`, `TestResolveAbsentKeyWithNoDefaultReturnsNoValue`, `TestResolveExplicitZeroValueBeatsLowerTierNonZeroValue`, `TestResolveEmptyEnvStringCounts`, `TestResolveProvenanceLabelsAreCanonical`, `TestResolveSourceSnapshotImmutability`, `TestResolveReusableSourceSnapshots`, `TestResolveConcurrentSourceReuse`, `TestNewFlagSnapshotUnknownKey`, `TestNewFlagSnapshotDuplicateBinding`, `TestNewFlagSnapshotNoPartialStateOnError`, `TestNewFlagSnapshotKindMismatch`, `TestNewFlagSnapshotSensitiveRedaction`, `TestNewFlagSnapshotSensitiveBindingErrorDoesNotEchoCorpus`, `TestNewFlagSnapshotNormalizedSetDuplicateBinding`, `TestSourceFlagBindingConstant`, `TestResolveFlagNotInFlagValuesDefersTolowerTiers`, `TestQAConfigResolutionWorkflowCoversFlagBindingAndFullPrecedence`, `TestQAConfigFlagBindingDiagnosticsCoverErrorCategories` | current |
| Typed config retrieval | FR16, FR20 | `Snapshot.IsSet` returns false for unregistered and absent keys, true for any set value including zero values, empty strings, and nil-typed defaults. Typed getters (`GetString`, `GetBool`, `GetInt`, `GetInt64`, `GetUint`, `GetUint64`, `GetFloat64`, `GetDuration`, `GetStringList`) return the value and nil error on success, or zero value and `*GetError` on failure. Three sentinel error categories cover all failure modes: `ErrKeyNotFound`, `ErrKeyAbsent`, `ErrGetConversion`. `*GetError` is inspectable with `errors.Is`/`errors.As`; sensitive key failures set `Redacted() true` and omit raw values from error strings. `GetStringList` returns a defensive copy. | `TestGetStringReturnedValueAndProvenance`, `TestGetBoolReturnedValue`, `TestGetIntReturnedValue`, `TestGetInt64ReturnedValue`, `TestGetUintReturnedValue`, `TestGetUint64ReturnedValue`, `TestGetFloat64ReturnedValue`, `TestGetDurationReturnedValue`, `TestGetStringListReturnedValue`, `TestGetStringListReturnsDefensiveCopy`, `TestGetKindMismatchReturnsGetConversionError`, `TestGetAbsentRegisteredKeyReturnsKeyAbsentError`, `TestGetUnregisteredKeyReturnsKeyNotFoundError`, `TestIsSetDistinguishesAbsentFromZero`, `TestIsSetReturnsTrueForExplicitZeroInt`, `TestIsSetReturnsTrueForEmptyStringFromExplicit`, `TestIsSetReturnsFalseForAbsentKey`, `TestIsSetReturnsFalseForUnregisteredKey`, `TestGetValueFromDefaultSource`, `TestGetValueFromAllSources`, `TestGetExplicitZeroValueCountsAsSet`, `TestGetEmptyEnvStringCountsAsSet`, `TestGetStringListEmptySliceCountsAsSet`, `TestGetSensitiveKeyAbsentError`, `TestGetSensitiveKeyKindMismatchError`, `TestGetErrorSentinelCategories`, `TestGetErrorAccessors`, `TestGetStringListNilVsEmpty`, `TestQAConfigTypedGettersWorkflowCoversAllKinds`, `TestQAConfigTypedGettersResolvedPresenceStates`, `TestQAConfigGetterDiagnosticsCoverAbsenceAndKindMismatch` | current |
| Config provenance and diagnostics | FR16, FR20, NFR3, NFR4, NFR8 | `Snapshot.SourceReport` returns deterministic value-free report entries in definition order, including absent registered keys. `Snapshot.WriteSourceReport` renders the report to caller-supplied writers without raw values. `InspectDiagnostic` exposes structured state for `*DefinitionError`, `*SourceError`, and `*GetError`; `WriteDiagnostic` renders deterministic value-free diagnostics that distinguish failure category from attempted source label. Sensitive corpus values never appear in reports, rendered diagnostics, public report formatting, or diagnostic formatting. | `TestSourceReportCoversWinningSourcesAndAbsentKeys`, `TestSourceReportRenderingIsDeterministicValueFreeAndWriterBound`, `TestSourceReportPublicFormattingNeverLeaksRawValues`, `TestInspectDiagnosticClassifiesConfigErrors`, `TestWriteDiagnosticIsDeterministicValueFreeAndWriterBound`, `TestDiagnosticFalsePositiveRedactionCoverage`, `ExampleSnapshot_SourceReport`, `TestQAConfigProvenanceReportsExplainWinningSourcesWithoutValues`, `TestQAConfigDiagnosticsDistinguishSourceAndCategory`, `TestQAConfigProvenanceRenderingRedactsSensitiveCorpus` | current |
| Compatibility familiarity | — | Behavior-scoped familiarity with Go `flag`, pflag, Cobra, and Viper differences is documented. Dib does not claim source compatibility with those APIs. | — | later: Epic 5 story 5.1 |
| Release-candidate evidence | — | Release readiness is validated with end-to-end dependency and provenance evidence. | — | later: Epic 5 story 5.4 |

## Current Scope Boundaries

This matrix tracks the implemented Epic 2 flag parser surface, Epic 3 command
routing/rendering/boundary surface, and Epic 4 config resolution surface. The
parser evidence map above is complete for the behavior areas implemented in
Stories 2.1 through 2.8. The command rows cover Story 3.1 root and nested
routing, Story 3.2 alias routing and validation, Story 3.3 command
local/inherited flag composition evidence, Story 3.4 deterministic help/usage
rendering, and Story 3.5 caller-controlled execution-boundary metadata. The
config rows cover Story 4.1 config key definitions and default snapshots, Story
4.2 explicit/env/JSON source ingestion, Story 4.3 flag binding and precedence
resolution, Story 4.4 typed getters, and Story 4.5 provenance reports and
diagnostics.

The matrix does not claim that compatibility tables, migration examples, or
release-candidate evidence are complete today. Epic 3 completed
caller-controlled boundary metadata; callback invocation, handler dispatch, and
process lifecycle ownership remain deferred unless a future architecture/API
decision explicitly approves that surface. Story 4.3 adds config flag binding and
cross-source precedence resolution. Story 4.4 adds typed config getters
(`GetString` through `GetStringList`), `IsSet`, and the `*GetError` error type
with `ErrKeyNotFound`, `ErrKeyAbsent`, and `ErrGetConversion` sentinels. Story
4.5 adds structured source reports, rendered source reports, structured
diagnostic inspection, and rendered config diagnostics.
